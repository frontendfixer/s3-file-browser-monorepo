{
  "name": "@repo/db",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "prisma": "^6.19.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0"
  }
}